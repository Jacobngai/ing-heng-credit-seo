---
interface Props {
  title: string;
  description: string;
  ctaText: string;
  ctaLink: string;
  secondaryCtaText?: string;
  secondaryCtaLink?: string;
  belowButtonsText?: string;
  variant?: 'primary' | 'dark';
}

const {
  title,
  description,
  ctaText,
  ctaLink,
  secondaryCtaText,
  secondaryCtaLink,
  belowButtonsText,
  variant = 'primary'
} = Astro.props;

const bgClass = variant === 'primary'
  ? 'bg-gradient-to-br from-primary-400 to-primary-600'
  : 'bg-gradient-to-br from-gray-900 to-gray-800';
---

<section class={`${bgClass} text-white rounded-2xl overflow-hidden`}>
  <div class="container-custom py-16 md:py-20">
    <div class="max-w-3xl mx-auto text-center space-y-6">
      <h2 class="text-3xl md:text-4xl lg:text-5xl font-heading font-bold">
        {title}
      </h2>
      <p class="text-lg md:text-xl text-white/90">
        {description}
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center pt-4">
        <a href={ctaLink} class="btn bg-white text-primary-700 hover:bg-gray-50 border-4 border-white/30 px-10 py-4 text-lg min-h-[60px]">
          {ctaText}
        </a>
        {secondaryCtaText && secondaryCtaLink && (
          <a
            href={secondaryCtaLink}
            class="btn bg-transparent border-4 border-white text-white hover:bg-white/10 px-10 py-4 text-lg min-h-[60px]"
          >
            {secondaryCtaText}
          </a>
        )}
      </div>
      {belowButtonsText && (
        <p class="text-sm text-white/70 pt-2" set:html={belowButtonsText} />
      )}
    </div>
  </div>
</section>
